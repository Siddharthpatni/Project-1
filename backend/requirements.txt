# ──────────────────────────────────────────────────────────────────────
# Vergabepilot.AI — backend dependencies
# Pinned versions tested on Python 3.11 (Linux/macOS/Docker).
# ──────────────────────────────────────────────────────────────────────

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic>=2.9.2
pydantic-settings>=2.5.2

# Database
sqlalchemy==2.0.35
psycopg[binary]==3.2.3
alembic==1.13.3

# Async task queue
celery==5.4.0
redis==5.1.1

# HTTP / async
httpx>=0.27.2

# LLM provider SDKs (used by LLMClient for fallback direct calls)
openai>=1.51.0
anthropic>=0.36.0

# Scraping / browser automation
playwright==1.47.0
lxml==5.3.0
beautifulsoup4==4.12.3
requests>=2.32.3

# Object storage (MinIO / S3)
boto3==1.35.36

# Utilities
python-multipart==0.0.12
tenacity==9.0.0
structlog==24.4.0
rich>=13.9.2

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-json-report==1.5.0
pytest-timeout==2.3.1

# Data processing
pandas==2.2.3
openpyxl==3.1.5

# ── Advanced CUA / AI agents ────────────────────────────────────────
# browser-use and langchain are optional for CI — the orchestrator uses
# lazy imports so tests pass even when these are unavailable.
browser-use==0.12.7
langchain-openai>=0.1.0
langchain-anthropic>=0.2.0
maincontentextractor==0.0.4

# ── Deep document extraction (no LLM) ───────────────────────────────
pdfplumber>=0.11.0        # PDF text + table extraction
pypdf>=4.0.0              # PDF fallback parser
python-docx>=1.1.0        # DOCX parsing and report generation
reportlab>=4.2.0          # PDF report generation
# OCR fallback for scanned PDFs — optional, gracefully skipped if absent
# Requires system packages: tesseract-ocr tesseract-ocr-deu poppler-utils
# Install with: apt-get install tesseract-ocr tesseract-ocr-deu poppler-utils
pytesseract>=0.3.10
pdf2image>=1.16.3
# Prometheus metrics
prometheus-client>=0.21.0
